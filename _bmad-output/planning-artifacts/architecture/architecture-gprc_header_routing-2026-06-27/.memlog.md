---
scope: Productionize grpc-routing-meta kit (example->production); brownfield; epics E1-E5 from PRD
purpose: Build substrate to keep E1-E5 from diverging during bmad-dev-story; + human-facing rendering at finalize
altitude: feature->epics (PRD has 5 epics; spine fixes the invariants the epics' stories share)
updated: 2026-06-28T09:25
---

- (event) inherited+constraint layer logged; opening coaching on open forks
- (constraint) CR1: wire bytes FROZEN. No SPEC.md changes. Only NEW header is x-routing-error (already provisional in SPEC 2/7), emitted iff a required scalar is empty. Digest/encoding/overflow/count semantics unchanged.
- (constraint) refs/ is read-only. Live docs to update are the grpc-routing-meta/ copies (CONTEXT.md, OVERVIEW.zh.md, README.md). Work only inside this workspace; local commits, no push.
- (constraint) Scope locked to PRD: P0 (FR1-8/NFR1-7) + plan.md 4 trimmed P1 items HR1-4. P2 excluded. No new runtime deps (keep hand-rolled url_encode/sha256; gRPC optional).
- (decision) [ADOPTED] Paradigm = codegen-driven projection pipeline: protoc plugin generates ProjectMeta(req,sink) projecting body->headers; one Send<>() template serves all systems; MetadataSink abstracts gRPC (VectorSink/GrpcSink). Settled by reality; ratify.
- (decision) [ADOPTED] F/NFR6 policy centralized: 3 limits (7168/25/512) live only in process_context_emit.h; exactly one plugin. SEAM: HPACK +32 duplicated (kHpackEntryOverhead in emit.h AND literal +32 in metadata_sink.h Add) -- candidate single-definition cleanup.
- (decision) [ADOPTED] FR4 build-time negative-codegen gate: plugin Validate() rejects bad (routing.project) + dup keys with non-zero protoc exit before emitting; build.sh asserts tests/negative/*.proto rejected. Keep; must run in CI.
- (direction) Working mode: full coaching. Deliverable: spine + human-facing rendering at finalize.
- (question) FORK-1 (load-bearing): kit public API boundary -- where do Send<>() + ProjResult/Issue live, and how does Send resolve the generated ProjectMeta for an arbitrary Req? Today Send is in the DEMO app (unified_sender.cc) and ProjectMeta is generated into GLOBAL namespace. plan.md: ProjectMeta in namespace routingmeta, found by ADL via the sink arg.
- (question) FORK-2: duration ownership. Send is timed (plan.md) AND ProjectMeta returns ProjResult{...,duration}. Who measures/populates duration, and what does it cover (ProjectMeta-only vs FillCommon+ProjectMeta)? bench_projection needs a stable answer.
- (question) FORK-3: CI protobuf pinning. Need 3.20 AND 3.21 deterministically x {gcc,clang} in GH Actions. Stock apt ships one version per Ubuntu release -> can't get both. Options: source-build at pinned tags (cached) vs container images vs vcpkg/conda baselines.
- (question) FORK-4: build.sh toolchain discovery (kill hardcoded anaconda path). Mechanism: env override (PROTOC/CXX) -> derive PB_INC/PB_LIB from protoc prefix, vs lean on pkg-config protobuf. CMake already uses find_package(Protobuf).
- (constraint) OWNERSHIP (corrected): protoc-gen-meta plugin + shared lib (metadata_options/process_context protos, routingmeta surface, generated ProjectMeta, policy) are AUTHORED by the Sys1/2/3 teams as a shared lib and DISTRIBUTED to the Sender team, who consumes and MAY locally modify. => distributed-library boundary, not internal.
- (decision) [DECIDED] Fork-1 boundary (option ii): lib contract = POPULATE + REPORT only -- generated ProjectMeta + FillCommon + ProjResult + policy/digest/overflow, all in namespace routingmeta. Send ORCHESTRATION is the Sender's; kit ships NO Send symbol; Send example lives in demo (unified_sender.cc) / README. Matches SPEC 7 report/decide line, extended to orchestration. Prevents per-consumer projection divergence via the single generic ProjectMeta path.
- (decision) [DECIDED] Fork-1 resolution (option A): generated ProjectMeta declared in namespace routingmeta; consumer's own Send resolves it via ADL on the routingmeta::MetadataSink arg (sink is always a routingmeta type -> ADL cannot be defeated by a proto's package). Public lib surface under src/common/ (ProjResult/Issue -> src/common/proj_result.h); generated .proj.h wraps output in routingmeta + includes kit headers. Distributed lib owns one coherent namespace.
- (decision) [DECIDED] Distributed-library boundary invariants: (1) routingmeta public API is a cross-distribution contract -> CR2 additive evolution is load-bearing: void->ProjResult MUST stay source-compatible for a consumer who ignores the return / does not recompile in lockstep. (2) projection-version drift between an independently-modified Sender and the receiver is detected by x-process-context-digest (SPEC 5.3) + x-contract-version (SPEC 8), NOT prevented. 'One sender / no divergence' benefit = provided + detectable, not enforced.
- (event) CONFLICT surfaced+clarified: 'Send defined in the kit (not demo)' exists ONLY in PRD FR6/G-E, NOT in read-only BRIEF. BRIEF criterion E requires only a branchless Send<>()=FillCommon+ProjectMeta path serving 3 systems, demonstrated by unified_sender (location-agnostic). So option (ii) does NOT violate the scored BRIEF; earlier 'criterion E at risk' was a FR6/E conflation, corrected.
- (question) ACTION (PRD reconciliation, owner=PM John, skill=bmad-edit-prd): relax FR6 + G-E -- drop 'defined in the kit (not the demo app)'; reframe lib contract = populate+report, Send orchestration = Sender's, kit ships no Send symbol. Timing TBD: (a) now, or (b) batched at architecture finalize. Read-only BRIEF untouched.
- (decision) [DECIDED] Fork-2 duration ownership (consequence of the populate+report boundary): generated ProjectMeta self-times its own projection and populates ProjResult.duration. bench_projection measures ProjectMeta directly (pure projection). The Sender's own Send does NOT time; it only reads ProjResult to decide. One timing point, no coordination. (FR7/criterion H.)
- (decision) [DECIDED] Fork-3 CI protobuf pinning: build each protobuf version FROM SOURCE at a pinned release tag, installed to a job-local prefix, cached via actions/cache keyed on the tag. Boring + self-contained: no reliance on distro packaging, version=one tag var. Matrix = GH Actions, Linux x {gcc,clang} x {protobuf 3.20, 3.21}. Each job: build + negative-codegen gate + 3 binaries + bench + gRPC compile-smoke (HR4).
- (assumption) Protobuf patch pins: 3.20.3 (matches local anaconda protoc per PRD 4) and 3.21.12 (last 3.21.x). [ASSUMPTION] web-verify the latest/last patch tags at finalize (finalize reviewer checks versions).
- (version) protobuf 3.20.3 + 3.21.12 (CI matrix, pinned tags); C++17; CMake find_package(Protobuf); GitHub Actions.
- (decision) [DECIDED] Fork-4 build.sh toolchain discovery (option A): no hardcoded paths. PROTOC=${PROTOC:-protoc} (PATH); CXX=${CXX:-c++}; include/lib via pkg-config protobuf (PB_CFLAGS/PB_LIBS), plugin appends -lprotoc. Env overrides are the escape hatch; pkg-config is the discovery mechanism, pairs with Fork-3 (CI sets PATH + PKG_CONFIG_PATH to the pinned prefix). CMake already uses find_package(Protobuf). build.sh is the no-cmake fallback. (A/NFR1.)
- (event) All 4 open forks decided; entering Finalize (distill spine from memlog).
- (event) Two reconciliation reviews logged. review-reconcile-spec-code: PASS-WITH-FINDINGS (spine ratifies code+SPEC accurately; only low nits). review-reconcile-brief-prd: PASS-WITH-FINDINGS with CRITICAL C1 (AD-4 overrides LOCKED plan.md P0.3 "promote Send into kit", misreported as PRD-only) + HIGH H1 (HR2 parser robustness silently dropped) + H2 (plan.md-fidelity constraint not carried).
- (decision) [DECIDED by user j] FORK-1 ratification: KEEP AD-4 (Send stays in demo; kit ships no Send symbol). This is a DELIBERATE deviation from locked plan.md P0.3, accepted with eyes open. Per the methodology-A/B fidelity rule, the deviation is now DECLARED, not silent: AD-4 marked [DECIDED — DEVIATES FROM plan.md P0.3; PENDING CROSS-TEAM RATIFICATION]; footnote rewritten to name the plan.md P0.3 + pivot-API conflict (not "PRD-only").
- (action) [PENDING, owner=PM John, skill=bmad-edit-prd, BEFORE bmad-create-epics-and-stories] Relax PRD FR6/G-E: drop "Send defined in the kit"; reframe lib contract = populate+report, Send orchestration = Sender's, kit ships no Send symbol. Read-only BRIEF untouched. This is the one remaining gate before epics/stories.
- (event) Finalize edits applied to spine (status draft->final): added Fidelity & Declared Deviations section (H2); AD-4 deviation declared + PENDING-RATIFICATION (C1); AD-5 extends no-throw to demo Send (M3); HR2 parser-robustness convention added + G-row ref (H1); CR3 read-only + no-push conventions added (M1/M2); AD-14 no-push note (M2); AD-6 sub-ms 1/2/25/60 bar (L2); AD-7 single-source home=leaf metadata_sink.h per include direction (L-2); AD-2 names FillCommon/Runtime as currently-global (L-1); canonical convention reworded Key=UrlEncode(Value) + NFR4 determinism named (L-3/L1); Pending Actions section gates epics/stories.
- (decision) [REVERSED per j, 2026-06-27] AD-4 reverted to plan.md P0.3: kit SHIPS one branchless routingmeta::Send (=FillCommon+ProjectMeta, returns ProjResult, no-throw); caller owns only abort/proceed decision (SPEC 7). Withdraws the earlier 'kit ships no Send symbol' deviation -> zero deviations, full plan.md fidelity. AD ID kept stable; rule amended in place. Rippled: Fidelity section, ownership map, dep diagram, AD-2/AD-5/AD-6, capability map E, structural seed, Deferred.
- (event) Downstream reverts queued: PRD v0.3 FR6/G-E -> back to 'Send in kit' (owner PM, bmad-edit-prd); epics.md Story 1.4/AR3/FR6 -> 'routingmeta::Send in kit' (owner SM). Re-run readiness after.
